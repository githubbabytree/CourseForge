import assert from "node:assert/strict";import test from "node:test";import{readFile}from"node:fs/promises";
test("online editors expose real revisions explicit diff apply locks restore and UTC+8",async()=>{const deck=await readFile(new URL("../app/deck-revision-editor.tsx",import.meta.url),"utf8");const material=await readFile(new URL("../app/material-revision-editor.tsx",import.meta.url),"utf8");const page=await readFile(new URL("../app/page.tsx",import.meta.url),"utf8");for(const marker of ["listDocumentRevisions","getRevisionContent","createRevisionProposal","applyRevisionProposal","restoreDocumentRevision","formatShanghaiDateTime"])assert.match(deck,new RegExp(marker));assert.match(deck,/updateRevisionLocks/);assert.match(deck,/TTS\/视频已过期/);assert.match(material,/kind:"material"/);assert.match(material,/sections\/\$\{index\}\/speakerNotes/);assert.match(page,/client.mode==="online"\?<MaterialRevisionEditor/);assert.doesNotMatch(deck,/设计草稿|假研究/);});
