{
  "name": "@courseforge/tts-worker",
  "version": "1.1.3",
  "private": true,
  "scripts": {
    "build": "python3 -m compileall -q src",
    "typecheck": "python3 -m compileall -q src test",
    "test": "PYTHONPATH=src python3 -m unittest discover -s test -v"
  }
}
