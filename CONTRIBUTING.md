# 贡献指南

CourseForge 当前采用受控源代码发布方式，未授予通用开源许可。外部贡献者提交代码前，应先与仓库所有者确认贡献与许可安排。

## 提交要求

1. 从最新 `main` 创建功能分支，不要直接推送主分支。
2. 不得提交 API Key、Token、Cookie、私钥、生产地址、内部材料、模型权重或生成产物。
3. Provider 密钥只能使用 `env://...` 或已实现的 `secret://...` 引用。
4. 新增迁移只能追加编号文件；已应用迁移及其校验和不得修改。
5. 行为变化必须附带测试，并保持 UI 和文档如实描述实际能力。

提交前执行：

```bash
npm ci
npm run verify
git diff --check
```

Pull Request 请用中文说明变更目的、影响范围、验证证据、安全边界和仍需人工验收的事项。
